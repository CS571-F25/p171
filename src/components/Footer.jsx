import React from "react";

export default function Footer() {
  return (
    <footer style={{ padding: "1.25rem", textAlign: "center" }}>
      <small>© LocalBite — Curated local food & events</small>
    </footer>
  );
}
